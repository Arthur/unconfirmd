<h1>Rumors controller, new action</h1>

<p>Edit this file in <tt>app/views/rumors/new.html.erb</tt></p>

<%= error_messages_for :rumor %>

<% form_for(@rumor, :action => url(:rumors) ) do |f| %>
  <p>
    <%= text_control :title, :label => 'Rumor' %>
    <%= text_control :author, :label => 'Author' %>
    
    <%= submit_button "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url(:rumors) %>
