%h2.rumor
  = link_to rumor.title, url(:rumor, rumor)
  =h rumor.score
  = link_to image('plus.png'), url(:plus_rumor, rumor)
  = link_to image('minus.png'), url(:minus_rumor, rumor)
  
.number_of_comments
  = rumor.comments.size
  = rumor.comments.size == 1 ? 'comment' : 'comments'
